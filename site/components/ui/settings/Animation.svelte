<script>
  import { Prop, Slider } from '/props';
  import { attach } from '/canvas';

  const { settings, update } = attach();

  export function tick (props) {
    const { frame } = props;
  }
</script>
